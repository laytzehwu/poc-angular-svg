export interface IDiagramSettings {
    default: {
        diagram: {
            height: number;
            width: number;
        },
        node: {
            width: number;
            marginWidth: number;
        }
    }
};
